fastapi>=0.109.0
uvicorn>=0.27.0
sse-starlette>=1.8.2
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.25

# Data validation
pydantic>=2.5.3

# LLM orchestration (direct calls, no framework overhead)
litellm>=1.30.0

# CrewAI tools only (web search / scrape — used by travel guide if API keys present)
crewai-tools>=0.17.0

# Amadeus flight & hotel API
amadeus>=9.0.0

# Testing
pytest>=8.0.0

# Web search tool
tavily-python>=0.5.0

# Frontend
streamlit>=1.30.0
requests>=2.31.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<4.1  # passlib is incompatible with bcrypt 4.1+

# HTTP client
httpx>=0.26.0

# Environment
python-dotenv>=1.0.0

dataclasses-json

# Map & Calendar
folium>=0.15.0
streamlit-folium>=0.18.0
geopy>=2.4.0
icalendar>=5.0.0

# Pinterest image scraping
pinterest-dl>=0.7.0

# Stripe payments
stripe>=7.0.0

# Anthropic Claude SDK
anthropic>=0.42.0

# OpenMeteo weather API
openmeteo-requests>=1.2.0
requests-cache>=1.2.0
